---
import Icon from './Icon.astro';
import { valuePoints } from '@data/site';
---

<section class="section section-shell why" id="about" aria-labelledby="why-title">
  <div class="why__content">
    <p class="eyebrow">Why Solwave</p>
    <h2 id="why-title">
      Agency delivery.
      <span>Enterprise standards.</span>
      <span>Startup pace.</span>
    </h2>
    <p>
      Solwave brings senior software engineering discipline to product teams that need
      dependable execution, clean architecture and practical momentum.
    </p>
  </div>

  <div class="why__visual" aria-hidden="true">
    <div class="brand-mark"></div>
  </div>

  <div class="why__points">
    {
      valuePoints.map((point) => (
        <article class="value-point">
          <div class="icon-tile">
            <Icon name={point.icon} />
          </div>
          <div>
            <h3>{point.title}</h3>
            <p>{point.description}</p>
          </div>
        </article>
      ))
    }
  </div>
</section>
